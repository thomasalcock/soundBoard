# Simple soundboard

A simple soundboard that plays back audio files and allows users to load files onto the pads. 
It depends on the awesome libraries raylib and tinyfiledialogs, they do most of the work.
